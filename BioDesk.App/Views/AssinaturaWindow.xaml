<Window x:Class="BioDesk.App.Views.AssinaturaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:BioDesk.App.Controls"
        Title="Assinatura Digital - BioDesk PRO" 
        Height="500" Width="700"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Icon="/Assets/Images/Logo.png">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Colors.xaml"/>
                <ResourceDictionary Source="../Styles/Typography.xaml"/>
                <ResourceDictionary Source="../Styles/Controls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{DynamicResource Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{DynamicResource Primary}" 
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                          Text="✍️" 
                          FontSize="24" 
                          VerticalAlignment="Center" 
                          Margin="0,0,15,0"/>
                
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Assinatura Digital" 
                              FontSize="20" 
                              FontWeight="SemiBold" 
                              Foreground="White" 
                              FontFamily="Segoe UI"/>
                    <TextBlock Text="Assine digitalmente o questionário de saúde" 
                              FontSize="14" 
                              Foreground="White" 
                              Opacity="0.9" 
                              FontFamily="Segoe UI"/>
                </StackPanel>
                
                <Button Grid.Column="2" 
                       Content="✕" 
                       Background="Transparent" 
                       Foreground="White" 
                       BorderThickness="0" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Padding="10" 
                       Cursor="Hand"
                       Click="FecharJanela_Click"
                       ToolTip="Fechar"/>
            </Grid>
        </Border>
        
        <!-- Content -->
        <Grid Grid.Row="1">
            <controls:SignatureCanvas x:Name="AssinaturaCanvas"
                                     SignatureCompleted="OnSignatureCompleted"
                                     SignatureCancelled="OnSignatureCancelled"/>
        </Grid>
    </Grid>
</Window>