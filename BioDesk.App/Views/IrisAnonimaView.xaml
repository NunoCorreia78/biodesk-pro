<UserControl x:Class="BioDesk.App.Views.IrisAnonimaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280"
             Background="{StaticResource BackgroundBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource HeaderStyle}">
            <Grid Margin="32,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Logo e Título -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="56" Height="56" 
                            Background="{StaticResource PrimaryBrush}"
                            CornerRadius="12"
                            Margin="0,0,16,0"
                            VerticalAlignment="Center">
                        <TextBlock Text="👁️" 
                                   FontSize="28"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Avaliação de Íris (Anónima)" 
                                   Style="{StaticResource HeroTitleStyle}"/>
                        <TextBlock Text="Análise iridológica sem identificação do paciente" 
                                   Style="{StaticResource HeaderSubtitleStyle}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Back Button -->
                <Button Grid.Column="2" 
                        Content="← Voltar" 
                        Style="{StaticResource PrimaryCTAButtonStyle}"
                        Click="BackToHome_Click"
                        Width="120"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="0,64,0,32">
            <Border MaxWidth="800" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="👁️" 
                               Style="{StaticResource IconStyle}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,24"/>
                    <TextBlock Text="Em Desenvolvimento" 
                               Style="{StaticResource CardTitleStyle}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16"/>
                    <TextBlock Text="Esta funcionalidade permitirá realizar análises iridológicas de forma anónima, carregar imagens da íris e sobrepor mapas de diagnóstico." 
                               Style="{StaticResource SubtitleStyle}"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               MaxWidth="500"
                               Margin="0,0,0,32"/>
                    <Button Content="← Voltar ao Início" 
                            Style="{StaticResource PrimaryCTAButtonStyle}"
                            Click="BackToHome_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>