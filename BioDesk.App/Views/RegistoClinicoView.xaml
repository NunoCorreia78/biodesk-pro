<UserControl x:Class="BioDesk.App.Views.RegistoClinicoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#FAFBFC">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Style for expandable cards -->
        <Style x:Key="ExpandableCardStyle" TargetType="Expander">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Opacity="0.04" ShadowDepth="2" BlurRadius="8"/>
                            </Border.Effect>
                            <StackPanel>
                                <ToggleButton Name="ExpanderButton" 
                                            IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                            Background="Transparent" 
                                            BorderThickness="0"
                                            Padding="20,16"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Left">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter Grid.Column="0" 
                                                        ContentSource="Header"
                                                        VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" 
                                                 Text="▼" 
                                                 FontSize="12"
                                                 VerticalAlignment="Center"
                                                 RenderTransformOrigin="0.5,0.5">
                                            <TextBlock.RenderTransform>
                                                <RotateTransform x:Name="ExpanderRotation" Angle="0"/>
                                            </TextBlock.RenderTransform>
                                        </TextBlock>
                                    </Grid>
                                </ToggleButton>
                                <ContentPresenter Name="Content" 
                                                Visibility="Collapsed"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="Content" Property="Visibility" Value="Visible"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ExpanderRotation"
                                                           Storyboard.TargetProperty="Angle"
                                                           To="180" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ExpanderRotation"
                                                           Storyboard.TargetProperty="Angle"
                                                           To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/> <!-- 70% main column -->
            <ColumnDefinition Width="3*"/> <!-- 30% sidebar -->
        </Grid.ColumnDefinitions>

        <!-- Main Column with Scroll -->
        <ScrollViewer Grid.Column="0" 
                     VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Disabled"
                     Padding="24,20,12,20">
            <StackPanel>
                
                <!-- 1. Identificação do Paciente -->
                <Expander Style="{StaticResource ExpandableCardStyle}" IsExpanded="True">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👤" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Identificação do Paciente" 
                                      FontFamily="Segoe UI" FontSize="16" FontWeight="SemiBold"
                                      Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel Margin="20,0,20,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Foto do paciente -->
                            <Border Grid.Column="0" 
                                   Width="80" Height="80" 
                                   CornerRadius="40" 
                                   Background="#E5E7EB" 
                                   Margin="0,0,16,0">
                                <TextBlock Text="👤" 
                                          FontSize="32" 
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"
                                          Foreground="#9CA3AF"/>
                            </Border>
                            
                            <!-- Dados do paciente -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding PacienteSelecionado.NomeCompleto, FallbackValue='Nome do Paciente'}" 
                                          FontFamily="Segoe UI" FontSize="18" FontWeight="SemiBold"
                                          Foreground="#1E293B"/>
                                <TextBlock Text="32 anos • Contacto: +351 912 345 678" 
                                          FontFamily="Segoe UI" FontSize="13" 
                                          Foreground="#64748B" Margin="0,4,0,0"/>
                                <TextBlock Text="📧 paciente@email.com" 
                                          FontFamily="Segoe UI" FontSize="13" 
                                          Foreground="#64748B" Margin="0,2,0,0"/>
                            </StackPanel>
                            
                            <!-- Botão editar -->
                            <Button Grid.Column="2" 
                                   Content="✏️ Editar Dados" 
                                   Background="Transparent" 
                                   BorderBrush="#D1D5DB" 
                                   BorderThickness="1"
                                   Padding="12,6" 
                                   FontSize="12"
                                   Command="{Binding EditarPacienteCommand}"/>
                        </Grid>
                        
                        <!-- Alertas de alergias -->
                        <Border Background="#FEF2F2" 
                               BorderBrush="#F87171" 
                               BorderThickness="1" 
                               CornerRadius="6" 
                               Padding="12" 
                               Margin="0,16,0,0">
                            <TextBlock Text="{Binding AlertasImportantes, FallbackValue='⚠️ Alergias: Penicilina, Aspirina'}" 
                                      FontSize="12" 
                                      Foreground="#DC2626" 
                                      FontWeight="Medium"/>
                        </Border>
                    </StackPanel>
                </Expander>

                <!-- 2. Motivo da Consulta -->
                <Expander Style="{StaticResource ExpandableCardStyle}" IsExpanded="True">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🎯" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Motivo da Consulta" 
                                      FontFamily="Segoe UI" FontSize="16" FontWeight="SemiBold"
                                      Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel Margin="20,0,20,20">
                        <ComboBox Text="{Binding MotivoConsulta, UpdateSourceTrigger=PropertyChanged}"
                                 IsEditable="True"
                                 ItemsSource="{Binding MotivosConsulta}"
                                 Background="White" 
                                 BorderBrush="#D1D5DB" 
                                 BorderThickness="1"
                                 Padding="12,8" 
                                 FontSize="14"/>
                    </StackPanel>
                </Expander>

                <!-- 3. História Clínica -->
                <Expander Style="{StaticResource ExpandableCardStyle}">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📚" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="História Clínica" 
                                      FontFamily="Segoe UI" FontSize="16" FontWeight="SemiBold"
                                      Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel Margin="20,0,20,20">
                        <TextBox Text="{Binding HistoriaClinica, UpdateSourceTrigger=PropertyChanged}"
                                Background="White" 
                                BorderBrush="#D1D5DB" 
                                BorderThickness="1"
                                Padding="12,8" 
                                FontSize="14" 
                                TextWrapping="Wrap"
                                AcceptsReturn="True" 
                                MinHeight="100"
                                VerticalScrollBarVisibility="Auto"/>
                        
                        <Button Content="➕ Adicionar Antecedente" 
                               Background="#059669" 
                               Foreground="White" 
                               BorderThickness="0"
                               Padding="12,8" 
                               FontSize="12" 
                               Margin="0,8,0,0"
                               HorizontalAlignment="Left"
                               Command="{Binding AdicionarAntecedenteCommand}"/>
                    </StackPanel>
                </Expander>

                <!-- 4. Exame Objetivo -->
                <Expander Style="{StaticResource ExpandableCardStyle}">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔍" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Exame Objetivo" 
                                      FontFamily="Segoe UI" FontSize="16" FontWeight="SemiBold"
                                      Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel Margin="20,0,20,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="Observação" FontWeight="Medium" FontSize="13" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding ObservacaoExame, UpdateSourceTrigger=PropertyChanged}"
                                        Background="White" BorderBrush="#D1D5DB" BorderThickness="1"
                                        Padding="8,6" FontSize="12" Height="80" TextWrapping="Wrap"
                                        AcceptsReturn="True"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Margin="8,0,8,0">
                                <TextBlock Text="Palpação" FontWeight="Medium" FontSize="13" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding PalpacaoExame, UpdateSourceTrigger=PropertyChanged}"
                                        Background="White" BorderBrush="#D1D5DB" BorderThickness="1"
                                        Padding="8,6" FontSize="12" Height="80" TextWrapping="Wrap"
                                        AcceptsReturn="True"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="2" Margin="8,0,0,0">
                                <TextBlock Text="Mobilidade" FontWeight="Medium" FontSize="13" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding MobilidadeExame, UpdateSourceTrigger=PropertyChanged}"
                                        Background="White" BorderBrush="#D1D5DB" BorderThickness="1"
                                        Padding="8,6" FontSize="12" Height="80" TextWrapping="Wrap"
                                        AcceptsReturn="True"/>
                            </StackPanel>
                        </Grid>
                        
                        <Button Content="📷 Anexar Imagem" 
                               Background="Transparent" 
                               BorderBrush="#D1D5DB" 
                               BorderThickness="1"
                               Padding="12,8" 
                               FontSize="12" 
                               Margin="0,8,0,0"
                               HorizontalAlignment="Left"
                               Command="{Binding AnexarImagemCommand}"/>
                    </StackPanel>
                </Expander>

                <!-- 5. Diagnóstico/Impressão Clínica -->
                <Expander Style="{StaticResource ExpandableCardStyle}">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🩺" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Diagnóstico/Impressão Clínica" 
                                      FontFamily="Segoe UI" FontSize="16" FontWeight="SemiBold"
                                      Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel Margin="20,0,20,20">
                        <TextBox Text="{Binding Diagnostico, UpdateSourceTrigger=PropertyChanged}"
                                Background="White" 
                                BorderBrush="#D1D5DB" 
                                BorderThickness="1"
                                Padding="12,8" 
                                FontSize="14" 
                                TextWrapping="Wrap"
                                AcceptsReturn="True" 
                                MinHeight="80"
                                VerticalScrollBarVisibility="Auto"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="Categorias:" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Button Content="Osteopatia" Background="#E0F2FE" Foreground="#0891B2" BorderThickness="0" 
                                   Padding="8,4" FontSize="11" Margin="0,0,4,0"/>
                            <Button Content="Quântica" Background="#F0FDF4" Foreground="#16A34A" BorderThickness="0" 
                                   Padding="8,4" FontSize="11" Margin="0,0,4,0"/>
                            <Button Content="Fitoterapia" Background="#FEF3C7" Foreground="#D97706" BorderThickness="0" 
                                   Padding="8,4" FontSize="11"/>
                        </StackPanel>
                    </StackPanel>
                </Expander>

                <!-- 6. Plano Terapêutico -->
                <Expander Style="{StaticResource ExpandableCardStyle}">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📋" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Plano Terapêutico" 
                                      FontFamily="Segoe UI" FontSize="16" FontWeight="SemiBold"
                                      Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel Margin="20,0,20,20">
                        <DataGrid ItemsSource="{Binding PlanoTerapeutico}"
                                 AutoGenerateColumns="False"
                                 CanUserAddRows="True"
                                 Background="White"
                                 BorderBrush="#D1D5DB"
                                 BorderThickness="1"
                                 GridLinesVisibility="Horizontal"
                                 HeadersVisibility="Column"
                                 FontSize="13">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Terapia" Binding="{Binding Terapia}" Width="2*"/>
                                <DataGridTextColumn Header="Data" Binding="{Binding Data, StringFormat=dd/MM/yyyy}" Width="*"/>
                                <DataGridTextColumn Header="Frequência" Binding="{Binding Frequencia}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <Button Content="➕ Adicionar Terapia" 
                                   Background="#059669" 
                                   Foreground="White" 
                                   BorderThickness="0"
                                   Padding="12,8" 
                                   FontSize="12" 
                                   Margin="0,0,8,0"
                                   Command="{Binding AdicionarTerapiaCommand}"/>
                            
                            <Button Content="📄 Gerar PDF" 
                                   Background="#3B82F6" 
                                   Foreground="White" 
                                   BorderThickness="0"
                                   Padding="12,8" 
                                   FontSize="12" 
                                   Margin="0,0,8,0"
                                   Command="{Binding GerarPdfCommand}"/>
                            
                            <Button Content="📧 Email" 
                                   Background="#7C3AED" 
                                   Foreground="White" 
                                   BorderThickness="0"
                                   Padding="12,8" 
                                   FontSize="12" 
                                   Margin="0,0,8,0"
                                   Command="{Binding EnviarEmailCommand}"/>
                            
                            <Button Content="📱 WhatsApp" 
                                   Background="#059669" 
                                   Foreground="White" 
                                   BorderThickness="0"
                                   Padding="12,8" 
                                   FontSize="12"
                                   Command="{Binding EnviarWhatsAppCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </Expander>

                <!-- 7. Notas de Evolução -->
                <Expander Style="{StaticResource ExpandableCardStyle}">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💬" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Notas de Evolução" 
                                      FontFamily="Segoe UI" FontSize="16" FontWeight="SemiBold"
                                      Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel Margin="20,0,20,20">
                        <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding NotasEvolucao}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#F8FAFC" 
                                               BorderBrush="#E2E8F0" 
                                               BorderThickness="1" 
                                               CornerRadius="8" 
                                               Padding="12" 
                                               Margin="0,0,0,8">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Data, StringFormat=dd/MM/yyyy HH:mm}" 
                                                          FontSize="11" 
                                                          Foreground="#64748B" 
                                                          FontWeight="Medium"/>
                                                <TextBlock Text="{Binding Nota}" 
                                                          FontSize="13" 
                                                          Foreground="#1E293B" 
                                                          TextWrapping="Wrap" 
                                                          Margin="0,4,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                        
                        <Button Content="➕ Adicionar Nota" 
                               Background="#059669" 
                               Foreground="White" 
                               BorderThickness="0"
                               Padding="12,8" 
                               FontSize="12" 
                               Margin="0,8,0,0"
                               HorizontalAlignment="Left"
                               Command="{Binding AdicionarNotaEvolucaoCommand}"/>
                    </StackPanel>
                </Expander>

            </StackPanel>
        </ScrollViewer>

        <!-- Sidebar (30% right column) -->
        <Border Grid.Column="1" 
               Background="White" 
               BorderBrush="#E2E8F0" 
               BorderThickness="1,0,0,0"
               Padding="20,20,24,20">
            <StackPanel>
                
                <!-- Última Consulta -->
                <Border Background="#F8FAFC" 
                       BorderBrush="#E2E8F0" 
                       BorderThickness="1" 
                       CornerRadius="8" 
                       Padding="16" 
                       Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="📅 Última Consulta" 
                                  FontFamily="Segoe UI" 
                                  FontSize="14" 
                                  FontWeight="SemiBold"
                                  Foreground="#1E293B" 
                                  Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding UltimaConsulta, StringFormat=dd/MM/yyyy, FallbackValue='15/12/2024'}" 
                                  FontSize="12" 
                                  Foreground="#64748B" 
                                  Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding ResumoUltimaConsulta, FallbackValue='Tratamento para dor lombar em curso. Paciente refere melhoria.'}" 
                                  FontSize="12" 
                                  Foreground="#374151" 
                                  TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Alertas -->
                <Border Background="#FEF2F2" 
                       BorderBrush="#F87171" 
                       BorderThickness="1" 
                       CornerRadius="8" 
                       Padding="16" 
                       Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="⚠️ Alertas" 
                                  FontFamily="Segoe UI" 
                                  FontSize="14" 
                                  FontWeight="SemiBold"
                                  Foreground="#DC2626" 
                                  Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding AlertasImportantes, FallbackValue='Alergia: Penicilina\nRestrições: Hipertensão'}" 
                                  FontSize="12" 
                                  Foreground="#B91C1C" 
                                  TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Ações Principais -->
                <StackPanel Margin="0,0,0,16">
                    <Button Content="📝 Gerar Prescrição" 
                           Background="#059669" 
                           Foreground="White" 
                           BorderThickness="0"
                           Padding="16,12" 
                           FontSize="14" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"
                           Command="{Binding GerarPrescricaoCommand}"/>
                    
                    <Button Content="📄 Exportar PDF" 
                           Background="#3B82F6" 
                           Foreground="White" 
                           BorderThickness="0"
                           Padding="16,12" 
                           FontSize="14" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"
                           Command="{Binding ExportarFichaCommand}"/>
                </StackPanel>

                <!-- Pesquisa -->
                <Border Background="#F8FAFC" 
                       BorderBrush="#E2E8F0" 
                       BorderThickness="1" 
                       CornerRadius="8" 
                       Padding="16" 
                       Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="🔍 Pesquisa" 
                                  FontFamily="Segoe UI" 
                                  FontSize="14" 
                                  FontWeight="SemiBold"
                                  Foreground="#1E293B" 
                                  Margin="0,0,0,8"/>
                        <TextBox Text="{Binding TextoPesquisa, UpdateSourceTrigger=PropertyChanged}"
                                Background="White" 
                                BorderBrush="#D1D5DB" 
                                BorderThickness="1"
                                Padding="8,6" 
                                FontSize="12"
                                Tag="Buscar em todas as notas..."/>
                    </StackPanel>
                </Border>

                <!-- Salvar Changes -->
                <Button Content="💾 Salvar Alterações" 
                       Background="#A4C3A2" 
                       Foreground="White" 
                       BorderThickness="0"
                       Padding="16,12" 
                       FontSize="14" 
                       FontWeight="SemiBold"
                       Visibility="{Binding TemAlteracoes, Converter={StaticResource BooleanToVisibilityConverter}}"
                       Command="{Binding SalvarCommand}"/>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>